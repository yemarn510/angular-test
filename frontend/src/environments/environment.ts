import { Environment } from "../app/models/common.model";


export const environment: Environment = {
  API_ENDPOINT: 'https://api.artic.edu/api/', // for local endpoint, for this project, is it the same as prod
};
