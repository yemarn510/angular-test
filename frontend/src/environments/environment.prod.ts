import { Environment } from "../app/models/common.model";


export const environment: Environment = {
  API_ENDPOINT: 'https://api.artic.edu', // for production endpoint, for this project, is it the same.
};
